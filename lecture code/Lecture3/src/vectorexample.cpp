/*
 * CS 106B/X, Marty Stepp
 * This program demonstrates using the Vector collection class.
 */

#include <iostream>
#include "console.h"   // required when using Stanford library
#include "vector.h"
using namespace std;

/*
 * Returns the number of elements of the given vector that are between
 * the given minimum and maximum values, inclusive.
 */
int countInRange(Vector<int> v, int min, int max) {
    return 0;
}

/*
 * Removes all occurrences of the given string from the given vector.
 * Example: removing all occurrences of "b" from
 * {"a", "b", "c", "b", "b", "a", "b"}
 * should produce
 * {"a", "c", "a"}
 */
void removeAll(Vector<string>& v, string s) {

}



int main() {
//    Vector<string> v2 {"a", "b", "c", "b", "b", "a", "b", "b"};
//    cout << "v2 before remove: " << v2 << endl;
//    removeAll(v2, "b");
//    cout << "v2  after remove: " << v2 << endl;

//    // test the countInRange function
//    Vector<int> v {28, 1, 17, 4, 41, 9, 59, 8, 31, 30, 25};
//    int count = countInRange(v, 10, 30);
//    cout << "count is " << count << endl;

    return 0;
}
